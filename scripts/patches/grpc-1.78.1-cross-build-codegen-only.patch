--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -53136,7 +53136,7 @@ if(gRPC_INSTALL)
     DESTINATION ${gRPC_INSTALL_CMAKEDIR}
     NAMESPACE gRPC::
   )
-  if(gRPC_BUILD_CODEGEN)
+  if(gRPC_BUILD_CODEGEN AND _gRPC_PLUGIN_LIST)
     install(EXPORT gRPCPluginTargets
       DESTINATION ${gRPC_INSTALL_CMAKEDIR}
       NAMESPACE gRPC::
